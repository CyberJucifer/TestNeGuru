<h1 class="nav title"><%= link_to('Test Ne Guru', tests_path) %></h1>

<% if user_signed_in? %>
  <p class="nav user">Welcome, <b><%= "#{current_user.first_name} #{current_user.last_name}" %></b></p>
  <p class="nav user"><%= link_to('Logout', destroy_user_session_path, method: :delete) %></p>
<% else %>
  <p class="nav user"><%= link_to('Log in', new_user_session_path) %></p>
  <p class="nav user"><%= link_to('Sign up', new_user_registration_path) %></p>
<% end %>