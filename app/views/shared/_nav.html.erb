<nav class="navbar navbar-light" style="background-color: lightgrey">
  <a class="navbar-brand" href="<%= tests_path %>"><%= t('.title') %></a>
  <% if user_signed_in? %>
    <div>
      <%= t('.welcome', first_name: current_user.first_name, last_name: current_user.last_name) %>
      <%= link_to(t('.logout'), destroy_user_session_path, method: :delete) %><br>
      <%= link_to(t('.badges'), badges_path) %><br>
      <%= link_to(t('.admin_mode'), admin_tests_path) if current_user.admin? %>
    </div>
  <% else %>
    <div>
      <%= link_to(t('.log_in'), new_user_session_path) %> |
      <%= link_to(t('.sign_up'), new_user_registration_path) %>
    </div>
  <% end %>
</nav>
