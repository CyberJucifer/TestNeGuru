<p><%= t('devise.mailer.email_changed.hello', email: @resource.email) %></p>

<p><%= t('.request_change_password') %></p>

<p><%= link_to t('.change_password'), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t('.ignore') %>'</p>
<p><%= t('.notification') %></p>
